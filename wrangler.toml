name = "failure-as-a-service"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Tip: when importing from GitHub via Cloudflare “Workers Builds”,
# Cloudflare will detect package.json + Wrangler configuration automatically.
